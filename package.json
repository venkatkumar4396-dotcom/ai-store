{
  "name": "nexusforge-ai-store",
  "version": "1.0.0",
  "description": "NexusForge — Complete AI Agent Marketplace & Automation OS",
  "private": true,
  "scripts": {
    "dev": "start-dev.bat",
    "start": "node dist/index.js",
    "build:backend": "npm --prefix backend run build",
    "build:frontend": "npm --prefix frontend run build",
    "build": "npx --package=prisma@6.9.0 prisma generate && cd backend && npm install --production=false && npm run build && cd .. && node -e \"fs.cpSync('backend/dist', 'dist', {recursive: true})\"",
    "commit": "git add . && git commit -m \"update: platform enhancements\" && git push origin main",
    "push": "git add . && git commit -m \"update: code changes\" && git push origin main",
    "deploy": "docker-compose up -d --build",
    "monitor": "node scripts/monitor.js",
    "setup": "cd backend && npm install && npx prisma generate && npx prisma db push && npm run seed && cd ../frontend && npm install"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "@prisma/client": "6.9.0",
    "bcrypt": "^5.1.1",
    "chokidar": "^4.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.2.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.0",
    "nodemailer": "^9.0.3",
    "pdf-parse": "^2.4.5",
    "pdfkit": "^0.19.1",
    "qrcode-terminal": "^0.12.0",
    "socket.io": "^4.8.1",
    "uuid": "^14.0.1",
    "prisma": "6.9.0",
    "whatsapp-web.js": "^1.26.1-alpha.3",
    "winston": "^3.17.0",
    "yahoo-finance2": "^3.15.3"
  },
  "overrides": {
    "prisma": "6.9.0",
    "@prisma/client": "6.9.0"
  },
  "keywords": [
    "ai",
    "agents",
    "marketplace",
    "nextjs",
    "express",
    "prisma",
    "ollama",
    "gemini"
  ],
  "author": "NexusForge Team",
  "license": "UNLICENSED"
}
